import { StyleSheet } from 'react-native';

import { hp } from 'src/utils';

export const styles = StyleSheet.create({
  list: {
    flex: 1,
  },
  contentContainer: {
    gap: hp(12),
  },
  footer: {
    height: hp(80),
  },
});
